#include <stdio.h>
#include "main.h"

/**
 * main - prints the largest of 3 integers
 * Return: 0
 */

int main(void)
{
	int a, b, c, largest;

	a = 972;
	b = -98;
	c = 0;
	largest = (
	if (a > b && b > c)
	{
	argest = a;
	}
	else if (b > a && a > c)
	{
	largest = b;
	}
	else
	{
	largest = c;
	}
	return (largest);
	);

	printf("%d is the largest number\n", largest);
	return (0);
}
