#include <stdio.h>
#include <stdlib.h>

/**
 * main - A programme thta print it name
 * @argv: Array input
 * @argc: size of array input
 *
 * Return: (0)
 */

int main(int argc, char *argv[])
{
	int c = 0;

	if (c < argc)
	printf("%s\n", argv[c]);

	return (0);
}
