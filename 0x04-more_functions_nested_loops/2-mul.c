#include <stdio.h>
#include "main.h"

/**
 * mul - A function that multiple two number
 * @a: the first number
 * @b: the second number
 * Return: mul value
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
